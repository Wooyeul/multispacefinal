<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	
	<select id="find_user_by_user_id" parameterType="java.lang.String" resultType="main.vo.UserVO">
		{call p_find_user_by_user_id(#{value})}
	</select>
	
	<!-- 
	<select id="find_user_by_user_id" parameterType="java.lang.String" resultType="main.vo.UserVO">
		SELECT * FROM UserT WHERE user_id=#{value}
	</select>
	 -->
</mapper>






